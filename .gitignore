node_modules
dist
.DS_Store
*.log
# Local folder for images and context ignored
local
# Local environment files — never commit real keys. .env.example IS committed.
.env
.env.local
.env.*.local
# Vercel
.vercel
